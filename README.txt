WorkTrack V23 Integration — MENU DISPLAY FIX

Target branch: V23-integration ONLY.
Do not upload this package to main.

This package preserves the current index.html and restores the menu overlay CSS.
The screenshot showed that the menu was rendering at the bottom because the menu CSS was missing.
