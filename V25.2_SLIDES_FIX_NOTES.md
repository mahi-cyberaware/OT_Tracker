# WorkTrack V25.2 — Home Update Slides Fix

Fixes based on mobile screenshots:
- Rebuilt all three update slide SVG assets as valid SVG files.
- Removed the invalid security.svg image source.
- Removed duplicate overlay text that caused cropping/visual overlap.
- Slides now preserve their full 2:1 aspect ratio instead of object-fit:cover cropping on mobile.
- First slide loads eagerly; other slides lazy-load.
- Bumped app/service-worker cache versions so V25.2 is picked up after deployment.
- Bumped update notification version to 25.2.

No Supabase migration is required.

Deploy to a new development branch first, test Preview, then merge to main.
