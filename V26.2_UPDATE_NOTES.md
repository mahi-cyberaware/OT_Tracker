# WorkTrack V26.2

- Fixed update-card overlap caused by background artwork that contained baked-in text.
- Update cards now use clean image-only backgrounds from `backgrounds/`.
- Added release history: V26.2, V26.1, V26, V25.4.
- The newest version is controlled from one `WORKTRACK_UPDATE` object in `app-v22.js`; future releases can add a new slide at the top while retaining previous releases.
- Background images remain separate files and can be replaced without editing the card text.
- Improved desktop footer column width/spacing while preserving the mobile centered layout.
- Service-worker cache bumped to V26.2.
