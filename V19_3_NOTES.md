# WorkTrack V19.3 — Menu & PWA Cache Fix

Fixes: deterministic menu section switching and a new service-worker cache version so installed PWAs receive the new JavaScript. No Supabase migration required.
